$("#image-upload-form").html("<%= escape_javascript(render 'order_products/image_upload_form') %>");
$('html, body').animate({
  scrollTop: $("#image-upload-form").offset().top
 }, 1000);

$("#upload-image-button").hide();
