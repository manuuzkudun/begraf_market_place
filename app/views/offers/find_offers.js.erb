$("#product-offers").html("");
$("#image-upload-form").html("");
$("#product-offers").html("<%= escape_javascript(render('product_offers')) %>");
$('html, body').animate({
  scrollTop: $("#product-offers").offset().top
 }, 1000);

$("#product-find-button").hide();

$(".offer").click( function(event) {
  var offer = event.currentTarget;
  $(offer).siblings().removeClass("offer-selected");
  $(offer).addClass("offer-selected");
  $(offer).find("input[type=radio]").prop("checked", true);
});
