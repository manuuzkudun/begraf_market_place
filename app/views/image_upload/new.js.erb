$("#image-upload-form").html("<%= escape_javascript(render 'image_upload/image_upload_form') %>");
$('html, body').animate({
  scrollTop: $("#image-upload-form").offset().top
 }, 1000);

$("#upload-image-button").hide();
